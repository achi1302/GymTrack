<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:name=".Application"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymTrack"
        tools:targetApi="31">
        <activity
            android:name=".ui.Workouts.WorkoutsFragment"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Exercises.ExerciseFragment"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Profile.ProfileFragment"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.StartActivty"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.Login.LoginActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Signup.SignupActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Workouts.start.WorkoutStartActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Test.TestActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.Test.exercises.TestExercisesActivity"
            android:parentActivityName=".ui.Test.TestActivity">
            <!-- Parent activity meta-data to support API level 15+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.Test.TestActivity" />
        </activity>



    </application>

</manifest>